# Metrics

- `learnings.jsonl` — session-scoped learnings managed by `/evolve`.
- `sessions.jsonl` — per-session execution summaries written by `/close`.

Schema source: session-orchestrator `skills/evolve/SKILL.md` and `skills/session-end/SKILL.md`.

Do not gitignore these files; they persist project intelligence across sessions.
